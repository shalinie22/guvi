#TO FIND A SEPARATE LIST OF PRIME NUMBERS AND NON PRIME NUMBERS 
#list for prime numbers
p=[]
#list for non prime numbers
np=[]
#get number from the user
n=int(input("enter the no:"))
for i in range (1,n):
#get the list of numbers from the user
  a=int(input("enter : "))
  if(a>1):
    for j in range (2,a//2+1):
      if((a%i)==0):
      #add the non prime numbers to a list 
        np.append(a)
        break
    else:
   #add the prime numbers to a list
      p.append(a)
 #print the prime number and non prime number lists 
print("list of prime no :",p)
print("list of nonprime no :",np)
          
